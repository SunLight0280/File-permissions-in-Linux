# File-permissions-in-Linux
Hands-on Linux security project focused on auditing and managing file and directory permissions using `ls -la` and `chmod`, applying the principle of least privilege to restrict unauthorized access.
